import { Typography } from "@mui/material";

const FormPage: React.FC = () => {
  return (
    <Typography variant="h4" component="h4" sx={{ textAlign: "center" }}>
      Здесь будет страница с формой
    </Typography>
  );
};

export default FormPage;
